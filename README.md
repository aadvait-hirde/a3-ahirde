# Assignment 3 – CSCI-A 290 PHP

This project contains a PHP script that demonstrates the use of functions for repeated operations. The script includes two functions: the first concatenates two strings with a tab character in between, and the second calculates the sum of diagonal elements in a 3x3 integer array. Both functions are called with sample inputs and their results are printed to the screen.
